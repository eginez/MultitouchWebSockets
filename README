MultitouchWebSockets
An application to use multitoutch hardware in web applications. It embeds a websocket server used to move
data from the hardware to the web app.

0.1: 
 - Very basic websocket server written in Objective C
 - supports only one client
 - broadcasting only one point

